# Authentication Secrets (Change these in production!)
SECRET_KEY=super-secret-key-change-in-prod
JWT_SECRET_KEY=jwt-secret-key-change-in-prod

# Database connection (Uncomment and replace with your Postgres credentials)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ownthemic
